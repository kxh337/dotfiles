# My Home

## Home manager:
Install using the following command:

```sh
home-manager switch --flake .#<config>
```
Where config is the PC name:
- chromebook
- desktop


## NixOS
Install using the following command:
```sh
sudo nixos-rebuild switch --flake .#<config>
```

Where config is the PC name:
- chromebook
- desktop
